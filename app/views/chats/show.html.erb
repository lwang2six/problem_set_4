<script language="JavaScript">
$(document).ready(
  function(){
    setInterval(function(){
      $('#messages').load("<%= url_for :action=> :load_messages %>");
    }, 2000);
  });
</script>

Available chat users:
<ul>
<% @chat.users.each do |user| %>
  <li><%= user.email %> </li>
<% end %>
<p>
</ul>
<div id="messages">
 <%= render @message %>
</div>
<div id="message_form">
<p>
<hr/>
<!--%= render 'form2' %-->
<%= link_to "new message", 
    new_chat_message_url(@chat, :format => :js),
    :remote => true, :id => "new_message_link" %>
</p>
</div>
