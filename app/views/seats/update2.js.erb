$("#<%= dom_id(@seat) %>").replaceWith('<%= link_to  "#{@seat.user.email}", @seat, :id => dom_id(@seat) %>');
$("#<%= dom_id(@old_seat) %>").replaceWith('<%= link_to "Empty Seat", upseat_url(@old_seat, :format => :js), :confirm => "Are you Sure?", :remote => true, :id => dom_id(@old_seat) %>');
